package org.gosspy.constants;

/**
 * Stores all the constants used across the files.
 */
public class Constants {
    /**
     * Name of the config file to be used. Assumes that the file is placed in resources folder.
     */
    public static final String APPLICATION_YAML_FILE = "application.yml";
}